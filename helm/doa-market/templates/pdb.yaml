{{- range $serviceName, $service := .Values.services }}
{{- $pdb := $service.podDisruptionBudget | default $.Values.defaults.podDisruptionBudget }}
{{- if and $service.enabled $pdb $pdb.enabled }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ $serviceName }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app: {{ $serviceName }}
    {{- include "doa-market.labels" $ | nindent 4 }}
spec:
  {{- if $pdb.minAvailable }}
  minAvailable: {{ $pdb.minAvailable }}
  {{- else }}
  maxUnavailable: {{ $pdb.maxUnavailable | default 1 }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ $serviceName }}
      {{- include "doa-market.selectorLabels" $ | nindent 6 }}
{{- end }}
{{- end }}
